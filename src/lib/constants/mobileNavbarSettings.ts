import type { DrawerSettings } from '@skeletonlabs/skeleton';

const mobileNavbarSettings: DrawerSettings = {
	id: 'mobile-navbar',
	bgBackdrop: 'bg-gradient-to-r from-primary-900 to-primary-800',
	bgDrawer: 'bg-white',
	width: 'w-full',
};

export default mobileNavbarSettings;
